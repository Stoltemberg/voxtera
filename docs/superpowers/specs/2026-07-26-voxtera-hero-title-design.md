# Voxtera hero title update

## Goal

Replace the hero message `Seu mundo voxel começa aqui` with the warmer
approved wording `Sua aventura começa aqui`.

## Copy design

- Update the visible hero `<h1>` to exactly `Sua aventura começa aqui`.
- Update the browser document title to `Voxtera — Sua aventura começa aqui`
  so the tab and shared page metadata express the same promise.
- Keep the hero artwork, navigation, CTA labels, direct `.exe` destination,
  layout, and all lower sections unchanged.

## Acceptance criteria

- The first-page headline reads `Sua aventura começa aqui`.
- The browser document title uses the same wording.
- Existing direct launcher download links remain `/downloads/VoxteraLauncher.exe`.
