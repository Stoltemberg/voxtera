{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Core window and event access for the main Voxtera launcher window.",
  "windows": ["main"],
  "permissions": ["core:event:default", "core:window:default"]
}
