{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Narrow window and progress-event access; filesystem, HTTP, and process execution stay behind typed Rust commands.",
  "windows": ["main"],
  "permissions": ["core:event:default", "core:window:default"]
}
